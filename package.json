{
  "name": "team-task-manager-root",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "cd backend && python -m venv venv && .\\venv\\Scripts\\pip install -r requirements.txt && cd ../frontend && npm install",
    "build": "cd frontend && npm run build",
    "start": "cd backend && .\\venv\\Scripts\\python app.py"
  }
}
